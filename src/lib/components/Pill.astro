<div
  class:list={[
    'flex gap-2 px-4 py-2',
    'whitespace-nowrap text-md leading-snug text-impact-text-over',
    'rounded-full border border-impact bg-impact'
  ]}>
  <slot />
</div>

{
  /*
<style>
  .pill {
    display: flex;
    padding: 0.5rem 1rem;
    background-color: var(--impact);
    border: 1px solid var(--impact);
    border-radius: 999rem;
    color: var(--impact-text-over);
    font-size: var(--text-md);
    line-height: 1.35;
    white-space: nowrap;
    gap: 0.5rem;
  }
</style>
  */
}
